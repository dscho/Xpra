diff -ur a/wimpiggy/lowlevel/__init__.py b/wimpiggy/lowlevel/__init__.py
--- a/wimpiggy/lowlevel/__init__.py	2009-11-08 19:17:54.000000000 +0700
+++ b/wimpiggy/lowlevel/__init__.py	2009-11-10 18:37:59.000000000 +0700
@@ -3,19 +3,28 @@
 # Parti is released under the terms of the GNU GPL v2, or, at your option, any
 # later version. See the file COPYING for details.
 
-from wimpiggy.lowlevel.bindings import *
 
+import sys
 from wimpiggy.log import Logger
 log = Logger()
 
+def noop(*args):
+    pass
+
+_sendClientMessage=noop
+if not sys.platform.startswith("win"):
+    from wimpiggy.lowlevel.bindings import *
+    _sendClientMessage = sendClientMessage
+
+
 def send_wm_take_focus(target, time):
     log("sending WM_TAKE_FOCUS: %r, %r", target, time)
-    sendClientMessage(target, False, 0,
+    _sendClientMessage(target, False, 0,
                       "WM_PROTOCOLS",
                       "WM_TAKE_FOCUS", time, 0, 0, 0)
 
 def send_wm_delete_window(target):
     log("sending WM_DELETE_WINDOW")
-    sendClientMessage(target, False, 0,
+    _sendClientMessage(target, False, 0,
                       "WM_PROTOCOLS",
                       "WM_DELETE_WINDOW", const["CurrentTime"], 0, 0, 0)
